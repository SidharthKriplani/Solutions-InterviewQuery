https://www.interviewquery.com/questions/top-3-users

SQL:

WITH CTE AS
(SELECT
RANK() OVER(PARTITION BY DATE ORDER BY DOWNLOADS DESC) DAILY_RANK,
DATE, DOWNLOADS, USER_ID
FROM DOWNLOAD_FACTS)

SELECT *
FROM CTE
WHERE DAILY_RANK < 4


PYTHON:

