https://www.interviewquery.com/questions/employee-salaries-etl-error

SQL:

SELECT FIRST_NAME, LAST_NAME, SALARY
FROM (
    SELECT ID, FIRST_NAME, LAST_NAME, SALARY, DEPARTMENT_ID, 
    DENSE_RANK() OVER(PARTITION BY FIRST_NAME, LAST_NAME ORDER BY ID DESC) AS RANKK
    FROM EMPLOYEES
    ORDER BY 2, 3
) A
WHERE RANKK = 1


PYTHON:

