https://www.interviewquery.com/questions/hr-salary-reporting

SQL:

SELECT JOB_TITLE, 
SUM(SALARY) AS TOTAL_SALARIES, 
SUM(OVERTIME_RATE*OVERTIME_HOURS) AS TOTAL_OVERTIME_PAYMENTS,
SUM(SALARY) + SUM(OVERTIME_RATE*OVERTIME_HOURS) AS TOTAL_COMPENSATION 
FROM EMPLOYEES
GROUP BY 1 
ORDER BY 4 DESC


PYTHON:

